apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: headscale-gateway
  namespace: headscale
spec:
  gatewayClassName: eg
  listeners:
  - name: http
    port: 80
    protocol: HTTP
    hostname: headscale.lac-coloc.fr
  - name: https
    port: 443
    protocol: HTTPS
    hostname: headscale.lac-coloc.fr
    tls:
      mode: Terminate
      certificateRefs:
      - kind: Secret
        name: headscale.lac-coloc.fr-cert
        namespace: headscale
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: headscale-cert
  namespace: headscale
spec:
  secretName: headscale.lac-coloc.fr-cert
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
  - headscale.lac-coloc.fr
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: headscale-https-route
  namespace: headscale
spec:
  parentRefs:
  - name: headscale-gateway
    namespace: headscale
    sectionName: https
  hostnames:
  - headscale.lac-coloc.fr
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /admin
    backendRefs:
    - name: headscale-admin
      port: 80
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: headscale
      port: 8080
---
# HTTPRoute pour HTTP (redirection 301)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: headscale-http-redirect
  namespace: headscale
spec:
  parentRefs:
  - name: headscale-gateway
    namespace: headscale
    sectionName: http
  hostnames:
  - headscale.lac-coloc.fr
  rules:
  - filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: https
        statusCode: 301
