apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring
resources:
  - service.yaml
  - statefulset.yaml
  - ingress.yaml
  - secret.yaml

configmapGenerator:
  - name: loki-config
    files:
      - loki.yaml


