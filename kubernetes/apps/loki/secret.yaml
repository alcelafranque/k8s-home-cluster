apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: loki
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: loki
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: k8s/dorado/applications/loki
