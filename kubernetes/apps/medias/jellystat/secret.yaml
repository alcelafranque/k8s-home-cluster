apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: jellystat
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: jellystat
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: k8s/dorado/applications/jellystat
