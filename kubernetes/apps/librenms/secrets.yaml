apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: librenms
  annotations:
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: librenms
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: k8s/dorado/applications/librenms
