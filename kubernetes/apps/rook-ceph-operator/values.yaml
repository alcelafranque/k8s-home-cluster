# Rook and CSI images follow the pinned rook-ceph chart (v1.20.7).
# Driver configuration moved to ../ceph-csi-drivers with Rook 1.20.
# Keep Ceph data-plane upgrades separate, in ../rook-ceph/values.yaml.
{}
