apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: syncthing-relay-keys
  namespace: syncthing-relay
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
